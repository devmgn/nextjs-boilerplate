import styled from 'styled-components';

const FieldLabel = styled.label`
  display: block;
  width: fit-content;
  font-size: 12px;
  line-height: 24px;
  color: #4c4d4d;
`;

export default FieldLabel;

import { IconGallery, IconItem, Meta } from '@storybook/blocks';
import SvgIcon from '../SvgIcon';
import * as Icons from '@/assets/icons';

<Meta title="Icon Gallery" />

<IconGallery>
  {Object.values(Icons).map((IconComponent, index) => {
    return (
      <IconItem name={IconComponent.name} key={index}>
        <SvgIcon as={IconComponent} />
      </IconItem>
    );
  })}
</IconGallery>

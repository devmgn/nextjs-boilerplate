import type { Transition } from 'framer-motion';

export const ROOT_CONTAINER_ID = '__next';
export const TRANSITION_DURATION = 0.3;
export const EASING = 'easeInOut';
export const TRANSITION: Transition = { duration: TRANSITION_DURATION, ease: EASING };

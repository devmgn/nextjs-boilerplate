import { keyframes } from 'styled-components';

export const fadeIn = keyframes`
  from {
    visibility: hidden;
    opacity: 0;
  }
`;

export const fadeOut = keyframes`
  to {
    visibility: hidden;
    opacity: 0;
  }
`;
